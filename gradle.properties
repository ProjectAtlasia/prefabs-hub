org.gradle.daemon=true
org.gradle.parallel=true
# Turn the following on once you finished changing your settings/build:
# org.gradle.configuration-cache=true
# org.gradle.caching=true

# Optional Hytale location for the Assets.zip resolution:
# hytale.home_path=...

# Contrato gRPC compartilhado: tag pinada do repo prefabs-uploader-proto.
# Bumpar: aponte pra tag nova e rode `./gradlew fetchProto`.
protoRef=v0.1.0
